import numpy as np, glob
for f in sorted(glob.glob("contrib/contrib_epoch*.npz")):
    d = np.load(f)
    gn = d["grad_norm"]
    al = d["align"]
    print(f, 
          "gn nan:", np.isnan(gn).sum(), "zero:", (gn==0).sum(), "min:", np.nanmin(gn),
          "| al nan:", np.isnan(al).sum())
